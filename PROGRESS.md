# PROGRESS

開発の進捗・残タスク・既知バグを管理するファイル。各フェーズ完了時に必ず更新する。
別セッションから再開する場合は、このファイルと DESIGN.md を最初に読むこと。

## フェーズ進捗

- [x] **Phase 0**: ゲームデザイン文書(DESIGN.md)作成 → **ユーザー承認待ち**
- [x] **Phase 1**: 走りのコア(カート物理・ドリフト・ミニターボ・カメラ・テスト用コース)※Fable直轄 — 実走検証済み
- [x] **Phase 2**: 本番コース3本、周回/順位判定、CPU AI — 3コース×8台の完走シミュレーション検証済み
- [x] **Phase 3**: アイテムシステム(10種+抽選+ボックス+AI使用) — 実レースで9種52回の使用を確認
- [x] **Phase 4**: 画面フロー・モード・HUD・キャラ9体 — GP3連戦→表彰式/TA/ポーズ全て検証済み
- [x] **Phase 5**: 演出・サウンド・最終レビュー・バグ修正・README — 全モード最終回帰テスト済み

## 現在の状態

- Phase 2 完了。3コース(cookieTown/chocoCanyon/skyCastle)+RaceManager+AIをSonnet 5体に委任し、
  Fableレビュー・統合後に全コースで8台フルレースをステップ実行シミュレーションで検証
  (クッキー: 全車完走112-118s/0落下、チョコ: 161s級/0落下、スカイ: 全車完走168-179s/落下36回=上級の意図難度)
- Fableによる統合後の重要修正(教訓):
  * AI壁際デッドロック: 旋回力∝速度の物理では壁際低速ブレーキ禁止 → 加速して向き直る
  * AIリカバリーモード: 接線と100°以上ズレ/壁ゾーンでドリフト解除+中心線へ滑らか復帰(バンバン制御禁止)
  * AIのfallZone認識: 早期壁補正+カーブ減速。gaps/jumpPadsは逆に全開直進
  * リスポーンは進行度保持(ゾーン先頭巻き戻しは後退が蓄積する)。ギャップのみ手前へ
  * ジャンプ台に最低射出速度22+飛行中センタリング補正(狭い足場の着地救済、人間にも有効)
- 検証ノウハウ: プレビュータブ非表示だとrAF停止→Game.app.updateFn(1/60)+Game.input.endFrame()を
  evalで直接ステップ実行して検証する(画面フロー含む全テストがヘッドレスで可能)
- Phase 3+4はSonnet 4体並列(items/characters/hud/screens)。hud.jsは報告のみ失敗したが実装は完全だった
- Fableによる統合修正: CapsuleGeometryはr134に無い→boot.jsにLatheGeometryシム追加/
  TAでアイテム未init時に前レースの見えないボックスが残留→Game.items.disable()追加
- Phase 5: audio.js(チップチューン6曲+SFX20種)+effects.js(パーティクル)をSonnet 2体で実装し、
  Fableがscreens/items/index.htmlへ配線(BGM切替・エンジン音・コールバックチェーン・爆発/紙吹雪)
- 最終レビュー(4観点探索、検証はセッション上限のためFableが直接実施)で5件修正:
  * newScene()のdisposeとitemsの共有リソースキャッシュの矛盾 → 毎レース再構築
  * リスタート時の旧エンジン音停止漏れ → _launchRaceでdetachAll
  * disable()の古いシーン参照残留 / clone()マテリアルのdispose漏れ / HUDの毎フレームquerySelector排除
- 最終回帰テスト: レース中リスタート→GP3連戦→表彰式→TA を通しで実行、コンソールエラーゼロ
- 全キャラ・コース・アイテム・BGMはオリジナル創作(名称・意匠の既存IP衝突は設計段階で審査済み)

## 実プレイフィードバック対応(第1回)

- 「コースが見えない」→ 路面メッシュの巻き順が逆(法線下向き→背面カリング)だった重大バグを修正。
  スクリーンショット検証パイプライン(Node受信サーバ+ブラウザから直接POST)を構築し、
  タイトル背景/レース中/スカイキャッスル/キャラ搭乗を目視確認済み
- iPadタッチ操作対応(touch.js)。非タッチ環境では自動無効
- 追加の見た目修正: 丸パーティクル化/チョコキャニオン明色化/キャラ搭乗スケール拡大
- 教訓: ヘッドレス(ロジック)検証だけでは描画バグを見逃す。以後の変更は必ずshot検証を通す

## 残作業・今後の拡張候補

- 実プレイでの手触りフィードバック反映(config.jsの定数調整で対応可能)
- スクリーンショットによる視覚確認(プレビュータブ非表示のため未実施。実プレイで確認を)
- 拡張候補: 難易度選択(ラバーバンド係数)、ゲームパッド対応、ゴースト機能、ミラーモード

## 残タスク

(各フェーズの詳細タスクは着手時にここへ展開する)

## 既知バグ

(なし)

## 開発ルール(要約)

- 実装の大部分は Sonnet サブエージェントに委任し、Fable がレビュー・統合する
- Phase 1 の物理・ドリフト・カメラは Fable 自身が実装する
- 各フェーズ完了時: ブラウザで実起動 → スクリーンショット確認 → コンソールエラーゼロ確認 → git commit
- ビルドツール不使用。index.html のダブルクリックで起動できる状態を維持する
- アセットは全てプロシージャル生成。外部画像・音源ファイルは使わない
- タイトル・キャラ名・アイテム名・コース名は全てオリジナル(任天堂の固有名詞・デザイン禁止)
